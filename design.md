# index.html 디자인 개선안

> 2026-08-05, 배포 후 실제 화면(1920×1080 이상 와이드 모니터) 스크린샷 검토 후 작성.
> 이 문서는 **진단 + 개선 방향 정리**이며, 아직 `index.html`에 반영되지 않았습니다. 방향에 동의하시면 코드에 적용합니다.

## 1. 문제 진단

### 1-1. 화면 비율 불균형 (가장 큰 문제)
- 실제 배포 화면 스크린샷 기준, 콘텐츠(카드①)는 전체 세로 길이의 약 20~25%만 차지하고 나머지 75%+ 는 빈 그라데이션 배경 + 파도 SVG만 있음.
- 원인: `body`가 `min-height:100vh`로 항상 화면 전체 높이를 채우는데, `.container`(콘텐츠)는 `max-width:860px`이고 분석 전에는 카드①(민원 입력)만 노출됨(카드②·③은 `display:none` → 분석 버튼을 눌러야 나타남). 그 결과 **와이드 모니터일수록** 콘텐츠와 배경 사이 불균형이 커짐.
- 부수 문제: 콘텐츠 폭이 항상 860px로 고정이라, 1920px 이상 화면에서는 좌우 여백도 과도하게 큼 → 페이지가 "화면 한가운데 떠 있는 작은 섬"처럼 보임.

### 1-2. "너무 단순함" — 랜딩 상태에 채울 내용이 없음
- 분석 버튼을 누르기 전 화면에는 제목 + 안내문 1줄 + 입력창 + 버튼이 전부. 이 도구가 뭘 할 수 있는지(16개 판단 항목, 어떤 근거 문서를 쓰는지) 미리 보여주는 내용이 없어 첫인상이 빈약함.
- 실제로는 근거 문서 2건, 판단 항목 16개, 답변 자동 조합이라는 꽤 풍부한 내용을 가진 도구인데 화면에 드러나지 않음.

### 1-3. 카드 디자인이 범용적("AI가 만든 듯한") 패턴에 가까움
- 흰 배경 + 상단 그라데이션 바 + 둥근 모서리 카드 + 중앙 정렬 제목 → 어디서나 흔히 보이는 템플릿형 구성.
- '부산 바다 + 친환경 전기차'라는 구체적 컨셉이 배경 그라데이션과 파도 SVG에만 있고, 카드 자체·타이포그래피에는 반영되어 있지 않음.

### 1-4. 타이포그래피가 넓은 공간에 비해 소극적
- 타이틀 28px, 안내문 14.5px — 화면 크기에 비해 존재감이 약해서 "빈 공간"이라는 인상을 더 키움.

## 2. 유지할 것 (이미 잘 되어 있음)
- 색상 컨셉(딥오션 → 웨이브 그라데이션 + 에코그린 포인트)은 부산 바다 + 친환경차라는 주제와 잘 맞음. **컨셉 자체는 유지.**
- 기능 자체(키워드 매칭 → 칩 on/off → 답변 초안, 직접 수정 가능)는 이미 실용적이고 잘 동작함. **로직은 건드리지 않음, 순수 레이아웃/비주얼 개선.**
- 다크 테마 고정(바다 컨셉)은 의도된 선택으로 유지 — 라이트/다크 자동 전환은 불필요.

## 3. 개선 방향

### 3-1. 색상 토큰 (기존 유지 + 보조 톤 추가)
| 이름 | 값 | 용도 |
|---|---|---|
| `--deep-ocean` | `#023047` | 배경 시작 (기존 유지) |
| `--ocean` / `--ocean-mid` / `--wave` | 기존 유지 | 배경 그라데이션 |
| `--eco-green` / `--eco-lime` | 기존 유지 | 포인트 컬러 |
| `--foam` | `#bdf2ef` | (신규 활용) 카드 테두리·구분선에 은은하게 사용해 "바다 거품" 느낌 강화 |
| `--ink-soft` | `#3a5a63` | (신규) 카드 내부 보조 텍스트, 현재 산발적으로 쓰이는 회색 톤 통일 |

### 3-2. 타이포그래피
- 표시(display) 역할: 현재 시스템 폰트 스택 유지하되 **타이틀 크기를 28px → 36~40px로 확대**, `text-wrap: balance` 적용.
- 본문 역할: 기존 유지, 다만 행간(`line-height`)을 살짝 넓혀 가독성 개선.
- 라벨/캡션 역할: 칩·문의처 등 소문자+자간(`letter-spacing`) 살려 현재 스타일 유지.

### 3-3. 레이아웃 개선 (핵심)
1. **콘텐츠 폭을 화면에 반응하도록 확장**: `max-width:860px` 고정 → 넓은 화면(1400px+)에서는 좌우 2열 구성으로 전환.
   - 왼쪽: 민원 입력 카드(①)
   - 오른쪽: **"이 도구가 판단하는 16개 항목" 미리보기 패널** — 분석 전에는 16개 카테고리 라벨을 정적 칩으로 나열해 보여주고, 분석 후에는 기존처럼 감지된 항목(②)·답변(③)으로 전환. → 랜딩 상태에서도 화면이 비어 보이지 않고, 이 도구의 범위를 한눈에 보여줌.
   - 좁은 화면(태블릿 이하)에서는 기존처럼 1열로 자동 전환(`grid-template-columns` 미디어쿼리).
2. **세로 공간 활용**: 히어로 영역에 "근거 문서 2건 · 판단 항목 16개 · AI API 미사용" 같은 요약 지표(스탯 타일 3개)를 배지 아래 추가해 상단부 존재감을 키움.
3. **`min-height:100vh` 강제 제거**: 콘텐츠 높이에 맞춰 배경이 자연스럽게 끝나도록 하고, 파도 SVG는 `position:fixed` 대신 콘텐츠 흐름 마지막에 배치해 "빈 공간 다음에 파도가 있는" 부자연스러움을 없앰.

### 3-4. 카드 비주얼 톤 변경
- 현재: 흰 배경 + 상단 컬러 바(accent bar) — 범용 패턴.
- 변경: 카드 배경을 완전한 흰색 대신 `--foam` 톤이 아주 옅게 섞인 톤(`#f7fdfd` 등)으로, 상단 accent bar 대신 **좌측 얇은 세로 라인 + 아이콘 배지** 조합으로 변경해 "파도가 밀려온 모래사장 위 카드" 느낌으로 차별화.
- 여백(`padding`)을 넉넉하게(현재 26px → 32~40px) 키워 확대된 카드 폭과 균형 맞춤.

### 3-5. 반응형 규칙
- `> 1400px`: 2열 레이아웃, 콘텐츠 최대 폭 1200px.
- `900px ~ 1400px`: 1열, 콘텐츠 최대 폭 860px(기존과 동일).
- `< 900px`: 기존 모바일 대응 그대로 유지(현재도 반응형 기본기는 있음 — 패딩/폰트 축소 정도만 점검).

## 4. 다음 단계
- [x] 위 방향에 대한 사용자 확인(2026-08-05)
- [x] `index.html`에 반영(2026-08-05) — 실제로 적용한 내용:
  - 헤더에 스탯 타일 3개(판단 항목 16 · 근거 문서 2 · 외부 API 호출 0) 추가, 타이틀 28px→38px 확대
  - `.layout` 그리드로 1080px 이상에서 좌(440px 입력 카드) / 우(1fr, 미리보기+감지항목+답변) 2열 구성, 그 미만은 기존처럼 1열
  - 우측 컬럼에 분석 전 상시 노출되는 "이 도구가 판단하는 16개 항목" 정적 미리보기 카드(`#preview-panel`) 추가, 분석 버튼 클릭 시 `hide` 클래스로 숨김
  - 카드 상단 가로 accent bar → 좌측 세로 accent bar로 변경, 카드 배경을 흰색에서 옅은 foam 톤(`--card-bg:#f8fdfc`)으로, 패딩 확대(26px→32px/34px)
  - `min-height:100vh`는 그대로 유지 — 대신 콘텐츠 자체를 늘려(2열+미리보기+스탯) 빈 공간 비율을 줄이는 방식으로 해결(파도 SVG가 `position:fixed`라 body를 억지로 줄이면 배경-파도 사이 이음새가 어색해질 위험이 있어, 더 안전한 방식 선택)
- [x] 재배포 완료(2026-08-05) — `public/index.html` 갱신 → `firebase deploy --only hosting` → https://electronic-carbot.web.app
- [ ] 실제 와이드 모니터 화면에서 사용자 육안 확인 (제가 가진 도구로는 스크린샷을 못 찍어 HTML 소스 레벨로만 검증함 — 사용자 확인 필요)
