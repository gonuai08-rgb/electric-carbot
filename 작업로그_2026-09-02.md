# 작업 로그 — 2026-09-02

전기차 민원 답변 도우미에 **근거 법령 조문을 GitHub Actions로 미리 받아 JSON으로 저장**하는 구조를 추가하고, GitHub 저장소 연결 및 재배포까지 완료.

---

## 1. 배경 / 목표

- 페이지 콘셉트 = "실행 중 외부 API 호출 0회, 이 페이지 안에서만 동작".
- 전기차 민원 근거 법령(대기환경보전법 등)은 **정해진 목록**이라 실시간 검색이 불필요.
- law.go.kr은 조문 본문을 JavaScript로 렌더링해 WebFetch로 원문을 못 가져옴.
- → **빌드 시점(GitHub 서버)** 에 법령을 받아 정적 JSON으로 저장하고, 웹페이지는 **같은 도메인의 그 파일만** 읽는 방식. 페이지는 여전히 외부 API 호출 0회, 파일이 없으면 수기 요약으로 폴백.

---

## 2. 추가/변경한 파일

| 파일 | 내용 |
|---|---|
| `scripts/fetch-laws.mjs` | 국가법령정보센터 Open API(DRF: `lawSearch.do` / `lawService.do`) 호출. Node 20+ 내장 `fetch`만 사용(외부 패키지 0). OC 키: `LAW_OC` 환경변수 > config > 공용키 `test`. |
| `scripts/laws.config.json` | 받을 법령 목록. `target=law`/`ordin` 구분, 조문 키(`58`, `79-4`)·별표 키(`21-2`) 지정. |
| `scripts/README.md` | 구성·OC 키·GitHub 준비 절차. |
| `.github/workflows/fetch-laws.yml` | `workflow_dispatch` + 매주 월 00:15 UTC(=월 09:15 KST) + 스크립트 변경 push 시 실행. 변경분만 `github-actions[bot]` 으로 커밋. `permissions: contents: write`, `concurrency: fetch-laws`. |
| `public/data/laws.json`, `data/laws.json` | 수집 결과(정적). Firebase Hosting이 서빙. |
| `index.html` / `public/index.html` | 법령 박스 안에 `#lawFetched`(수집 결과) + `#lawManualSummary`(기존 수기 요약). 하단 스크립트가 `fetch('data/laws.json')` → 성공 시 조문 원문을 `<pre>` 로 렌더링하고 수기 요약을 하위 `<details>` 로 접음. 실패 시 수기 요약 노출. |
| `firebase.json` | `hosting.site = "hello-carbot"` 추가. |
| `.gitignore` | 신규(`.firebase/`, `.claude/settings.local.json`, `setup-github.ps1`, `deploy.ps1`). |
| `setup-github.ps1`, `deploy.ps1` | 로컬 일회성 스크립트(영문 전용, gitignore). |
| `CLAUDE.md`, `tasks.md`, `작업내용.md` | 기록 갱신. |

### 수집 대상 법령 (5건, 조문 26개)

- 대기환경보전법 제58조·제58조의2 — 저공해차 보급·구매 지원(보조·융자) 상위 근거
- 대기환경보전법 시행규칙 제79조의4 + **별표 21의2**(사용기간별 지원금액 회수기준: 6개월 미만 70% …) — 의무운행기간 **8년** 근거
- 환경친화적 자동차법 제2조·제10조·제10조의2
- 환경친화적 자동차법 시행령 제18조·제18조의2·제19조
- 부산광역시 환경친화적 자동차 보급 촉진 및 이용 활성화에 관한 조례(전체 17조, 담당: 탄소중립정책과 051-888-3561)

---

## 3. GitHub 연결

- **설치**: Git 2.55.0.3, GitHub CLI 2.98.0 (winget).
- **인증**: `gh auth login`(device flow) → 이후 `.github/workflows/*` 푸시에 `workflow` 스코프가 필요해 `gh auth refresh -s workflow` 추가 실행. 계정 `gonuai08-rgb`.
- **저장소**: https://github.com/gonuai08-rgb/electric-carbot (public). `setup-github.ps1` 로 생성.
  - 1차 시도 실패: 스크립트 내 한글이 콘솔 인코딩(CP949)으로 깨져 `--description` 에 제어문자가 섞임 → `GraphQL: Description control characters are not allowed`. 스크립트를 **영문 전용**으로 재작성 후 성공.
  - 첫 `git push` 거부: `refusing to allow an OAuth App to create or update workflow ... without workflow scope` → 위 `gh auth refresh -s workflow` 로 해결.
- **Actions 권한**: `gh api -X PUT repos/.../actions/permissions/workflow -f default_workflow_permissions=write`.

---

## 4. 워크플로 실전 검증에서 잡은 문제 2가지

### (1) 동시 실행 레이스
push 트리거 실행이 도는 도중 수동 실행을 겹쳐 두 번째 `git push` 가 `! [rejected] (fetch first)` 로 실패.
→ 커밋 단계에 `git pull --rebase --autostash` 후 push, **3회 재시도** 추가.

### (2) law.go.kr 해외 IP 간헐 차단
한 CI 실행에서 5건 전부 `fetch failed` → 스크립트가 **정상 `laws.json` 을 오류본으로 덮어쓰고 커밋**(`77bb875`).
→ 스크립트 보강:
- `getJson`: 4회 재시도(지수 백오프) + 20초 타임아웃(`AbortSignal.timeout`).
- 수집 실패한 법령은 **이전 `laws.json` 의 정상 항목으로 대체**(`staleFromPrevious: true`), warnings에 기록.
- 사용가능 항목이 0이면 파일 미변경 + `exit 1`(워크플로 실패 표시).
- `laws` 내용이 이전과 같으면(= `fetchedAt` 만 변경) 저장 건너뜀 → 무의미한 주간 커밋 방지.

정상본은 커밋 `11c4031` 로 복구. 재검증 실행에서 `재시도 1/3` 후 5/5 정상 수집 → `내용 변화 없음, 파일 미갱신` → 워크플로 success, 불필요 커밋 없음.

> 남은 흔적: history 에 오류본 커밋 `77bb875` 존재하나 이후 커밋이 내용 복구 → 현재 트리·기능 영향 없음.

---

## 5. 배포 (사이트 주소 변경)

- `firebase.json` 에 `hosting.site = "hello-carbot"`.
- `firebase hosting:sites:create hello-carbot` → `firebase deploy --only hosting` (파일 2개: `index.html`, `data/laws.json`).
- **새 공개 주소: https://hello-carbot.web.app** — 배포 후 확인: `index.html` 200(로더 포함), `data/laws.json` 200(5법령·26조문).
- 옛 주소 `electronic-carbot.web.app` 는 2026-08-05 버전으로 **동결**(Firebase 기본 사이트라 삭제 불가, 앞으로 배포는 `hello-carbot` 으로만).
- Firebase 프로젝트명은 `electronic-carbot` 유지. → 이름 3개가 다름: 저장소 `electric-carbot` / 프로젝트 `electronic-carbot` / 사이트 `hello-carbot`.

---

## 6. 새로 설치한 것

```
winget install --id Git.Git          # git 2.55.0.3
winget install --id GitHub.cli       # gh 2.98.0
```
(Node.js·firebase-tools 는 2026-08-05 세션에서 이미 설치됨)

---

## 7. 남은 일 / 선택지

- 옛 주소 `electronic-carbot.web.app` 처리 방침 — 방치 / 최신본 배포 / 리다이렉트.
- (선택) open.law.go.kr 에서 전용 OC 키 발급 → GitHub Secret `LAW_OC` 등록(공용키 `test` 의존 제거).
- (선택) `laws.json` 갱신 시 Firebase 재배포까지 GitHub Actions 로 연결(현재는 저장소만 갱신, 배포는 수동).
- 별표 21의2 표 텍스트 정렬이 많이 깨지면 텍스트 대신 PDF 링크만 노출로 조정.
- 실제 민원 원문 추가 검증(기존 과제 계속).
