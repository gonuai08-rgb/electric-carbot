# 작업내용

> 최신 작업이 맨 위에 오도록 날짜별로 기록합니다.

---

## 2026-09-02

### 1. 오늘 한 일 — 법령 조문 원문을 GitHub Actions로 미리 받아 JSON으로 저장
배경: 페이지 콘셉트("실행 중 외부 API 호출 0회")를 유지하면서 법령 원문을 넣고 싶다는 요청.
law.go.kr은 조문 본문을 JavaScript로 렌더링해 WebFetch로는 원문을 못 긁는다.
→ **빌드 시점(GitHub 서버)** 에 법령을 받아 정적 JSON으로 저장하고, 페이지는 같은 도메인의 그 파일만 읽는 방식으로 구현.

1. **`scripts/fetch-laws.mjs`** 신설 — 국가법령정보센터 Open API(DRF: `lawSearch.do`/`lawService.do`)를 호출.
   Node 20+ 내장 `fetch`만 사용(외부 패키지 0). OC 키는 `LAW_OC` 환경변수 > config > 공용키 `test`.
   - `target=law`(법령)와 `target=ordin`(자치법규)의 응답 구조가 달라 각각 파서를 둠
     (law: `법령.조문.조문단위` + `항/호/목`, `별표.별표단위`; ordin: `LawService.조문.조` + `조내용` 통짜 문자열).
   - 조문 키 정규화(`0021`/`02` → `21-2`), 별표는 `별표구분='별표'`만, 별표내용의 `", "` 줄바꿈 아티팩트 정리.
2. **`scripts/laws.config.json`** — 받을 법령 목록. 대기환경보전법 58·58의2 / 시행규칙 79의4 + **별표 21의2(사용기간별 지원금액 회수기준)** / 친환경차법 2·10·10의2 / 같은 법 시행령 18·18의2·19 / **부산광역시 환경친화적 자동차 보급 촉진 및 이용 활성화에 관한 조례**(전체 17조).
   - 부산 자치법규는 "전기자동차 보급 촉진 및 지원 조례"라는 이름이 없어서, `ordin` 검색으로 실제 명칭(위)을 찾아 교체.
3. **`.github/workflows/fetch-laws.yml`** — `workflow_dispatch` + 매주 월 00:15 UTC(=월 09:15 KST) + 스크립트 변경 push 시 실행. `public/data/laws.json`·`data/laws.json` 변경분만 `github-actions[bot]`으로 커밋. `permissions: contents: write`.
4. **`index.html` 수정** (root + `public/` 동기화)
   - 법령 박스(`<details class="law-detail">`) 안에 `#lawFetched`(수집 결과) + `#lawManualSummary`(기존 수기 요약) 두 영역.
   - 하단 스크립트가 `fetch('data/laws.json')` → 성공 시 조문 원문을 `<pre>`로 렌더링하고 수기 요약을 하위 `<details>`로 접음. 실패(`file://` 로컬 열람, 파일 없음 등) 시 수기 요약이 그대로 노출.
   - 같은 도메인 정적 파일 읽기이므로 "외부 API 호출 0회" 배지·문구는 유지.
5. **로컬 시드 실행** — `node scripts/fetch-laws.mjs` 로 5/5 법령 수집, `public/data/laws.json`(44 KB) 생성. 별표 21의2 회수요율 표(6개월 미만 70% … )도 텍스트로 들어옴 + PDF 링크.
6. `scripts/README.md` 신설(설정·OC 키·GitHub 준비 절차).

### 2. 새로 설치한 것
없음. (Node.js는 지난 세션에 이미 설치됨. git은 이 PC에 없지만 이번 작업엔 불필요 — 사용자가 GitHub 연결 시 필요)

### 3. 확인·검증
- DRF API 공용키 `test` 로 정상 동작 확인(법령·자치법규 모두). 조문 텍스트는 현행(대기환경보전법 시행규칙 시행 2026-07-15 등).
- `index.html` 인라인 스크립트 문법 검사 통과(node vm). `<div>` 28/28, `<details>` 1/1 균형.
- 대기환경보전법 시행규칙 제79조의4 원문 확인: **의무운행기간 = 8년**(법 제58조제3항제1호 지원), 회수기준은 별표 21의2 위임.

### 4. GitHub 연결·배포 (이어서 진행)
- Git 2.55 / GitHub CLI 2.98 설치(winget). `gh auth login` + `gh auth refresh -s workflow`(워크플로 파일 푸시에 `workflow` 스코프 필요)를 device flow로 완료 — 계정 `gonuai08-rgb`.
- **저장소 생성·푸시:** https://github.com/gonuai08-rgb/electric-carbot (public). `setup-github.ps1`(영문 전용, gitignore)로 자동화 — 1차 시도는 스크립트 한글이 콘솔 인코딩으로 깨져 저장소 설명에 제어문자가 섞여 실패 → 영문으로 재작성 후 성공.
- Actions 워크플로 권한을 `write` 로 설정(`gh api ... /actions/permissions/workflow`).
- **워크플로 실전 검증:** push 트리거 실행 성공(`0f6afcd` 커밋 생성됨) → 파이프라인 동작 확인. 이후 두 가지 문제를 잡음:
  1. **동시 실행 레이스** — push 실행 도중 수동 실행을 겹쳐 두 번째 push가 `! [rejected] fetch first` 로 실패. → 커밋 단계에 `git pull --rebase` + 3회 재시도 추가.
  2. **law.go.kr 해외 IP 간헐 차단** — 한 실행에서 5건 전부 `fetch failed` 나며 정상 `laws.json` 을 오류본으로 덮어씀(`77bb875`). → ① `getJson` 4회 재시도(백오프)+20s 타임아웃 ② 실패한 법령은 이전 `laws.json` 의 정상 항목으로 대체(`staleFromPrevious`) ③ 사용가능 0건이면 파일 미변경+`exit 1`. 정상본은 `11c4031` 로 복구.
- 재검증 실행: `재시도 1/3` 후 5/5 정상 수집 → `내용 변화 없음, 파일 미갱신` (무의미 커밋 없음). 워크플로 success.
- **매주 월 09:15(KST) 자동 실행** 예약됨. 실 사용 시 open.law.go.kr 무료 OC 키를 Secret `LAW_OC` 로 넣으면 공용키 `test` 의존 제거.

### 5. 남은 것 (사용자 실행)
- `firebase deploy --only hosting` — `public/index.html`(법령 로더 포함) + `public/data/laws.json` 를 공개 사이트에 반영. (제 실행은 외부 배포라 차단됨)
- 배포 후 https://electronic-carbot.web.app 하단 "관련 법령 조문 원문" 펼쳐 조문 표시 육안 확인.
- 별표 21의2 표 정렬이 많이 깨지면 텍스트 대신 PDF 링크만 노출로 조정 검토.
- history 에 오류본 커밋 `77bb875` 이 남아 있으나 `11c4031` 로 내용 복구됨(기능 영향 없음).

---

## 2026-08-05

### 1. 오늘 한 일
1. **매칭 정확도 검증(가상 시나리오)** — 실제 민원 원문이 아직 없어 현실적인 문의 9건을 만들어 PowerShell로 키워드 매칭 로직을 시뮬레이션. 누락 5건 발견·수정: `youth`/`vulnerable`에 "생애최초" 추가, `repurchase_limit`에 "재구매/여러 대/두 대째/한 대 더" 등 추가, `mandatory_operation`에 "전출/판매/이전등록" 추가(기존엔 "전출하면서" 같은 표현이 `residence`로 잘못 매칭됐었음), `local_discount`에 "할인" 단독 키워드 추가, `period`에 "언제" 단독 키워드 추가.
2. **실제 민원 원문 1건 검증** — "아파트 단지 충전기 2대뿐, 추가 설치 요청": `repurchase_limit`의 "2대" 키워드가 충전기 대수에도 오탐(false positive)되는 버그 발견 → "2대 이상"/"2대를"로 구체화해 수정. 이 민원 자체는 "충전인프라" 문제로 현재 KB(구매보조금 16개 항목) 범위 밖임을 확인(근거 문서에 "충전" 언급 0건) → 사용자 확인 후 **지금은 보류**로 결정.
3. **실제 민원 원문 2건째 검증** — "보조금 조기마감으로 지원 못 받음, 추가 물량 요청": 키워드는 걸렸지만 본문에 "마감 이후 대응 방법"이 아예 없는 **콘텐츠 공백**을 발견. 부산시 공고 원문(27~28행, 54~55행)을 근거로 새 KB 항목 `budget_exhausted`(예산소진/조기마감 이후 대응 — 잔여물량 10/1 통합집행, 9월 이후 한국환경공단 국비 경로)를 신설.
4. **배포(1차, Claude Artifact)** — `index.html`을 Claude Artifact로 게시(비공개, 기존 디자인 그대로 유지).
5. **배포(2차, Firebase Hosting)** — 비전공자인 사용자를 위해 6단계로 나눠 확인받으며 진행: Node.js 설치 → Firebase CLI 설치 → 로그인(별도 대화형 창 필요) → 프로젝트 생성(여러 차례 오류 끝에 `electronic-carbot`으로 확정) → 배포 설정 파일 작성 → 배포. 최종 공개 주소 **https://electronic-carbot.web.app** (상세 과정은 "4. 막힌 것" 참고).
6. **레이아웃 디자인 개선** — 배포 화면이 "너무 단순하고 비율이 안 맞는다"는 스크린샷 피드백을 받아 `design.md`로 진단·개선안을 먼저 정리(승인 받음) 후 `index.html`에 반영: 헤더 스탯 타일 3개 추가, 1080px 이상에서 좌(입력)/우(미리보기+결과) 2열 그리드, "이 도구가 판단하는 16개 항목" 정적 미리보기 카드 신설, 카드 비주얼(좌측 accent bar, foam 톤 배경) 변경.
7. **법령 원문 링크 추가** — 「환경친화적 자동차의 개발 및 보급 촉진에 관한 법률」·「대기환경보전법 시행규칙」 제79조의4를 국가법령정보센터(law.go.kr) 링크로 footer에 추가. URL을 임의로 만들지 않고 WebSearch·WebFetch로 실제 페이지 존재를 먼저 확인한 뒤 반영.
8. **법령 링크 UI 세부 조정** (사용자 피드백에 따라 순차 반영) — 민원 입력창 확대(130px→220px), 법령 링크 2개를 보라색 필(pill) 버튼으로 변경(기존 색이 배경과 비슷해 안 보인다는 피드백), 안내 라벨 텍스트 자체도 law.go.kr 링크로 연결, "국가법령정보센터..." 문구를 빨간색 강조 → 범위를 문구 전체로 넓히고 글자 크기 확대(11px→14px) → 최종적으로 검정색으로 변경(배경과 대비가 약할 수 있음을 안내, 사용자 재확인 대기 중).
9. 위 변경 사항은 수정할 때마다 `public/index.html` 갱신 → `firebase deploy --only hosting` → `Invoke-WebRequest`로 배포된 실제 HTML을 직접 조회해 반영 여부를 검증하는 방식으로 진행(WebFetch 15분 캐시, 브라우저 캐시 때문에 "반영 안 됨"으로 오인된 해프닝이 있어 하드리프레시를 안내함).

### 2. 새로 설치한 것
이번 세션에서 아래 두 가지를 새로 설치했습니다 (그 외 설치 명령 없음 — `winget` 자체는 이미 설치되어 있던 것이라 새로 설치한 것 아님):

```powershell
winget install OpenJS.NodeJS.LTS --accept-package-agreements --accept-source-agreements --silent
```
→ Node.js LTS v24.19.0 설치 (Firebase CLI 실행에 필요, 설치 전엔 이 컴퓨터에 Node.js가 전혀 없었음)

```powershell
npm install -g firebase-tools
```
→ Firebase CLI v15.25.1 전역 설치

### 3. 새로 만든 파일과 용도
| 파일 | 용도 |
|---|---|
| `design.md` | 배포된 화면의 레이아웃/비율 문제 진단과 개선 방향을 정리한 디자인 계획 문서 |
| `public/index.html` | Firebase Hosting 배포 전용 폴더 — `index.html`을 복사해 넣음(근거자료·작업일지 등이 함께 배포되지 않도록 분리) |
| `firebase.json` | Firebase Hosting 배포 설정(`public` 폴더를 배포 대상으로 지정) |
| `.firebaserc` | 사용할 Firebase 프로젝트(`electronic-carbot`)를 지정하는 설정 파일 |
| `firebase-debug.log` | Firebase CLI가 오류 시 자동 생성한 디버그 로그(의도적으로 만든 파일 아님, 삭제해도 무방) |

기존 파일 중 `index.html`은 오늘 여러 차례 수정(KB 키워드/콘텐츠 보완, 레이아웃 개편, 법령 링크 추가·스타일 조정), `tasks.md`·`CLAUDE.md`도 진행 상황에 맞춰 계속 갱신했습니다.

### 4. 막힌 것
- 이 환경의 PowerShell 도구는 TTY(대화형 콘솔)가 없어 `firebase login`/`firebase login:ci` 같은 브라우저 인증이 필요한 명령을 직접 실행할 수 없었음 → 별도의 대화형 PowerShell 창을 새로 띄워 사용자가 직접 로그인하도록 우회.
- Firebase 프로젝트 생성 과정에서 서로 다른 원인의 오류가 연쇄적으로 발생: ① 한글 표시 이름이 GCP 규칙 위반("invalid characters") → 영문으로 변경, ② Google Cloud 서비스 약관 미동의 → 사용자가 console.cloud.google.com에서 동의, ③ (별개로) Firebase 자체 서비스 약관 미동의로 `addFirebase` 403이 반복 → 사용자가 console.firebase.google.com에서 별도 동의. 이 과정에서 사용자가 콘솔 UI로 진행하다가 원래 CLI로 만들어둔 `busan-ev-complaint-helper`에 연결하지 않고 **새 프로젝트(`electronic-carbot`)를 만들어버려**, 결국 이 프로젝트를 그대로 쓰기로 함. `busan-ev-complaint-helper`는 미사용 상태로 남아 있음(삭제 안 함, 무료라 방치 가능).
- 이 환경엔 스크린샷/헤드리스 브라우저 도구가 없어(chromium-cli 없음) 제가 화면을 직접 눈으로 볼 방법이 없음 — 배포된 HTML 소스를 직접 조회해 텍스트·스타일 반영 여부만 확인하는 방식으로 대체 검증함.
- `WebFetch` 도구의 15분 캐시 때문에 방금 배포한 내용을 확인할 때 구버전이 보여 "반영 안 됨"으로 오인한 적 있음 → `Invoke-WebRequest`로 직접 조회해 우회.
- Firebase Hosting 응답의 `Cache-Control: max-age=3600` 때문에 사용자 브라우저에 이전 배포본이 캐시되어, 재배포 후에도 화면엔 바로 안 보이는 일이 여러 번 있었음 → 강력 새로고침(Ctrl+Shift+R)·시크릿 창으로 해결 안내.
- law.go.kr의 특정 조문(제79조의4) 딥링크는 자바스크립트 렌더링 사이트라 WebFetch로 실제 동작 여부를 검증할 수 없어서, 검증된 법령 전체 페이지로만 링크하고 조문 번호는 텍스트로만 표기함.
- 「환경친화적 자동차의 개발 및 보급 촉진에 관한 법률」·「대기환경보전법 시행규칙」 원문 자체(로컬 파일)는 여전히 미확보 — 이번엔 law.go.kr 링크만 검증해서 추가한 것이고 조문 원문을 저장한 것은 아님.
- 법령 강조 텍스트를 검은색으로 바꾼 뒤, 배경(진한 바다색)과 대비가 약해 잘 안 보일 수 있다고 안내했으나 아직 사용자 확인 답변 없음.
- 단순 부분일치(substring) 키워드 매칭의 한계 확인: "이번에 또 한 대 사려고 합니다"처럼 핵심 단어가 문장 안에서 떨어져 있으면 여전히 못 잡음. 형태소 분석 등이 필요한데 "API 없이 순수 규칙 기반"이라는 설계 방향과 안 맞을 수 있어 보류, 담당자의 수동 체크박스 보정에 맡김.

### 5. 다음에 할 일
- [ ] 검은색으로 바꾼 법령 강조 텍스트가 배경과 잘 구분되는지 확인 후 필요시 색상 재조정
- [ ] 넓은 화면/좁은 화면에서 2단 레이아웃이 실제로 잘 전환되는지 사용자가 직접 육안 확인
- [ ] 미사용 상태인 `busan-ev-complaint-helper` GCP 프로젝트 정리 여부 결정
- [ ] 「환경친화적 자동차의 개발 및 보급 촉진에 관한 법률」·「대기환경보전법 시행규칙」 원문 자체 확보(로컬 저장) 여부 결정 — 현재는 law.go.kr 링크만 있음
- [ ] 「2026년 전기자동차 지역할인제 참여업체 모집 공고」, 2026년 상반기 보급사업 공고문 확보 여부 결정
- [ ] 충전인프라(충전기 설치) 민원 대응 방향 — 현재 보류, 관련 공고/지침 확보되면 재검토
- [ ] 추가 실제 민원 사례로 매칭 정확도 계속 검증

---

## 2026-07-29

### 1. 오늘 한 일
1. CLAUDE.md 최초 작성 — 프로젝트 역할(부산광역시 전기차 보조금 민원 답변 자동화), 근거문서 우선순위, 답변 작성 규칙(공문서체, 요지→근거→결론→문의처 순) 정의.
2. 부산시 2026년 하반기 전기자동차 보급사업 공고를 웹으로 조사 시도 — 부산시 홈페이지(busan.go.kr) 고시공고 게시판이 자바스크립트 기반 검색이라 원문 직접 수집은 실패, 보도자료·뉴스 기사로 개요(보급대수, 지원금액, 신청기간 등)만 우선 확보.
3. 사용자가 부산시 공고문 원본 PDF(부산광역시 공고 제2026-2121호, 29쪽)를 채팅에 직접 업로드 → 내용을 `reference/부산시_2026년_하반기_전기자동차_보급사업_공고.md`로 정리·저장.
4. 기후에너지환경부 「2026년 전기자동차 보급사업 보조금 업무처리지침」 PDF를 ev.or.kr에서 다운로드해 `reference/` 폴더에 저장. 이후 사용자가 동일 문서의 공식본(기후에너지환경부 공고 제2026-5호, 48쪽)을 채팅에 직접 업로드하여 같은 문서임을 재확인.
5. 민원 답변 자동화 "프로그램" 형태 논의 → 간단한 로컬 웹페이지(창 하나), "파란 바다 + 친환경 전기차" 컨셉으로 방향 확정.
6. `index.html` v1 제작 — Claude API(`claude-opus-5`)를 브라우저에서 직접 호출하는 방식. API 키는 사용자 브라우저 localStorage에 저장.
7. 사용자 요청("api 없이")으로 API 호출 방식 전면 폐기 → `index.html` v2로 재작성: 부산시 공고 + 기후부 지침의 핵심 조항을 16개 카테고리 키워드-매칭 지식베이스로 코드에 직접 내장한 **오프라인 규칙 기반** 답변 생성기로 전환. 감지된 항목을 체크박스(칩)로 켜고 끌 수 있게 구현.
8. `index.html` v3 — 민원 원문에서 앞부분 1~2문장을 추출해 답변 맨 위에 **【민원 요지】** 줄로 자동 삽입하는 기능 추가(관련 항목명도 함께 표시).

### 2. 새로 설치한 것
**이번 세션에서 새로 설치한 프로그램/패키지는 없습니다.** (npm/pip/choco 등 설치 명령 없음)

유일하게 실행한 외부 리소스 다운로드 명령은 아래와 같습니다 (기후부 지침 PDF를 `ev.or.kr`에서 내려받은 것 — 이후 사용자가 같은 문서의 공식 업로드본으로 재확인해 주셨습니다):

```powershell
New-Item -ItemType Directory -Force -Path "c:\Users\admin\Desktop\전기차\reference" | Out-Null
try {
    Invoke-WebRequest -Uri "https://ev.or.kr/nportal/file/downloadJfile.do?FILE_MASK=20260105091A89D4906C69474AAB63F7E4BD11E742" -OutFile "c:\Users\admin\Desktop\전기차\reference\2026년_전기자동차_보급사업_보조금_업무처리지침.pdf" -UseBasicParsing -TimeoutSec 30
    Get-Item "c:\Users\admin\Desktop\전기차\reference\2026년_전기자동차_보급사업_보조금_업무처리지침.pdf" | Select-Object Name, Length
} catch {
    Write-Output "ERROR: $($_.Exception.Message)"
}
```

`index.html`을 확인용으로 브라우저에서 여는 데 사용한 명령(설치 아님, 단순 실행):
```powershell
Start-Process "c:\Users\admin\Desktop\전기차\index.html"
```

### 3. 새로 만든 파일과 용도
| 파일 | 용도 |
|---|---|
| `CLAUDE.md` | Claude Code가 이 프로젝트에서 따라야 할 규칙(역할, 근거문서 우선순위, 답변 작성 규칙, 지금 단계) 정의 |
| `reference/부산시_2026년_하반기_전기자동차_보급사업_공고.md` | 부산시 공고 제2026-2121호 원문(사용자 업로드 PDF)을 마크다운으로 정리한 근거 자료 |
| `reference/2026년_전기자동차_보급사업_보조금_업무처리지침.pdf` | 기후에너지환경부 공고 제2026-5호 원문(전국 공통 보조금 산정 기준·법적 근거) |
| `index.html` | 민원 답변 초안 생성 웹페이지 (현재 v3: **API 없이** 규칙 기반 키워드 매칭 + 민원 요지 자동 추출 기능 포함, 브라우저에서 파일을 열기만 하면 동작) |
| `작업내용.md` | 이 문서 — 세션별 작업 로그 |
| `tasks.md` | 프로젝트 전체 작업 체크리스트 |

※ `.claude/settings.local.json`은 Claude Code 하네스가 자동 생성한 권한 설정 파일로, 제가 직접 작성한 산출물은 아닙니다.

### 4. 막힌 것
- 부산시 홈페이지(busan.go.kr) 고시공고 게시판이 자바스크립트 기반 검색이라 자동 크롤링이 안 됨 → 사용자가 PDF를 직접 업로드해 주셔서 해결.
- 「환경친화적 자동차의 개발 및 보급 촉진에 관한 법률」(친환경자동차법) 조문 원문 **아직 미확보** — 로컬 파일 없이, 조문이 필요할 때마다 국가법령정보센터 조회에 의존해야 하는 상태.
- 「2026년 전기자동차 지역할인제 참여업체 모집 공고」, 2026년 상반기 보급사업 공고문 **아직 미확보**.
- index.html v1(API 방식)은 API 키가 없어 실제 호출 테스트를 하지 못한 채로 폐기됨 — 이후 v2에서 API 방식 자체를 없앴으므로 더 이상 문제 아님(기록용으로만 남김).
- 규칙 기반 키워드 매칭이 완벽하지 않음 — 민원 문장에 해당 키워드가 없으면 관련 항목을 놓칠 수 있어, 담당자가 체크박스로 직접 켜고 끄도록 보완해 둔 상태.
- "민원 요지" 기능은 진짜 요약이 아니라 원문 앞부분 문장 추출 방식이라, 민원이 장황하거나 핵심이 뒷부분에 있으면 요지가 어색할 수 있음.

### 5. 다음에 할 일
- [ ] 실제 민원 사례 여러 건을 `index.html`에 넣어보고 키워드 매칭/문구가 실무에 맞는지 검증
- [ ] 검증 결과에 따라 지식베이스(KB) 항목·키워드 보완
- [ ] 친환경자동차법 조문, 지역할인제 참여업체 공고, 상반기 공고문 확보 여부 결정
- [ ] `index.html`을 다른 담당자와 공유할 방법 결정 (파일 그대로 공유 vs 사내 웹서버 게시 등)
